import { BlogType } from "../../hooks/hooks.types";

type Props = {};

const SingleBlogContentDetails = ({ data }: BlogType) => {
  return <div>SingleBlogContentDetails</div>;
};

export default SingleBlogContentDetails;
