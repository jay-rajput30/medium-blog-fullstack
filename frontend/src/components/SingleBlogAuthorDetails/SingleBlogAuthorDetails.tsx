import { BlogType } from "../../hooks/hooks.types";

const SingleBlogAuthorDetails = ({ data }: BlogType) => {
  return <div>SingleBlogAuthorDetails</div>;
};

export default SingleBlogAuthorDetails;
